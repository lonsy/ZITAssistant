<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="@android:textStyle">normal</item>
        <item name="@android:attr/actionBarSize">48dp</item>
        <item name="android:actionBarStyle">@style/AppTheme.ZITSActionBar</item>
    </style>
    
    <style name="AppTheme.ZITSActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">#3accb5</item>
        <item name="android:displayOptions">showCustom</item><!-- 默认显示CustomView,防止启动时的闪烁 -->
    </style>        
    
    <style name="CustomCheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
		<item name="android:button">@drawable/checkbox</item>
	</style>

</resources>
